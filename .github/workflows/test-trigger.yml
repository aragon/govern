name: test
on:
  push:
    paths:
      - 'packages/govern-console/**'
      - 'packages/govern/**'
    branches:
      - 'DAO-377-govern-console-ci-trigger'

env:
  working-directory: packages/govern-console

jobs:
  build-deploy-develop:
    runs-on: ubuntu-latest
    steps:
      - name: test echo
        run: echo triggered
