import DAOAction from '../../../internal/actions/DAOAction';

// Mocks
//jest.mock('');

/**
 * DAOAction test
 */
describe('DAOActionTest', () => {
  let action;

  beforeEach(() => {
    action = new DAOAction();
  });

  it('constructor check', () => {

  });
});