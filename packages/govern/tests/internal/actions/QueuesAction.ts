import QueuesAction from '../../../internal/actions/QueuesAction';

// Mocks
//jest.mock('');

/**
 * QueuesAction test
 */
describe('QueuesActionTest', () => {
  let action;

  beforeEach(() => {
    action = new QueuesAction();
  });

  it('constructor check', () => {

  });
});