import GamesAction from '../../../internal/actions/GamesAction';

// Mocks
//jest.mock('');

/**
 * GamesAction test
 */
describe('GameActionTest', () => {
  let action;

  beforeEach(() => {
    action = new GamesAction();
  });

  it('constructor check', () => {

  });
});